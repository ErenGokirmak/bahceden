<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/long_button_background"
    android:backgroundTint="@color/white"
    android:elevation="5dp"
    android:layout_margin="8dp"
    android:padding="8dp">

    <ImageView
        android:id="@+id/layoutCreditCartIcon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_credit_card"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="4dp"
        app:tint="@color/bahceden_green"/>

    <TextView
        android:id="@+id/layoutCreditCardTitle"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        app:layout_constraintBottom_toTopOf="@id/layoutCreditCardNumber"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@+id/layoutCreditCartIcon"
        android:text="My debit card"
        android:textSize="20sp"
        android:layout_marginStart="16dp"
        android:textColor="@color/black"
        android:fontFamily="@font/nexa_heavy"/>

    <TextView
        android:id="@+id/layoutCreditCardNumber"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginTop="4dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/layoutCreditCardTitle"
        app:layout_constraintStart_toEndOf="@+id/layoutCreditCartIcon"
        android:text="**** **** **** 6391"
        android:textSize="16sp"
        android:layout_marginStart="16dp"
        android:fontFamily="@font/nexa_heavy"/>

    <ImageView
        android:id="@+id/layoutCreditCartSelected"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:src="@drawable/ic_tick_unselected"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="4dp"
        app:tint="@color/bahceden_green"
        android:layout_marginVertical="4dp"/>


</androidx.constraintlayout.widget.ConstraintLayout>